name: CI
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rojo (robust)
        run: |
          set -euxo pipefail
          ROJO_VERSION=7.6.1
          TMP="$(mktemp -d)"
          cd "$TMP"
          curl -fsSL -o rojo.zip "https://github.com/rojo-rbx/rojo/releases/download/v${ROJO_VERSION}/rojo-${ROJO_VERSION}-linux-x86_64.zip"
          sudo apt-get update -y
          sudo apt-get install -y unzip
          unzip -o rojo.zip
          # Ensure the binary is named and on PATH
          sudo mv rojo /usr/local/bin/rojo
          sudo chmod +x /usr/local/bin/rojo
          rojo --version

      - name: Build place
        run: rojo build -o build/RobloxSoccer.rbxlx

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: place
          path: build/RobloxSoccer.rbxlx
