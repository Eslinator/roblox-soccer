{
  "dry_run": false,
  "steps": [
    { "type": "validate",
      "checks": [
        { "op": "cmd_contains", "cmd": "git rev-parse --abbrev-ref HEAD", "substr": "bot/setup-connector" }
      ]
    },
    { "type": "run", "cmd": "echo git tag -a v0.2.0 -m \"Roblox Soccer v0.2.0\"" },
    { "type": "run", "cmd": "echo git push origin v0.2.0" }
  ]
}
