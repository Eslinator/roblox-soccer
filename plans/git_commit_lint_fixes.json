{
  "dry_run": false,
  "steps": [
    { "type":"git_ops","ops": [
      { "op":"ensure_repo" },
      { "op":"create_branch", "name":"bot/setup-connector", "base":"main", "if_exists":"checkout" },
      { "op":"add", "paths":[
        "src/ServerScriptService/Net/RateLimiter.lua",
        "src/ServerScriptService/Net/InitRemotes.server.lua",
        "src/ServerScriptService/Match/MatchManager.server.lua"
      ]},
      { "op":"commit", "message":"chore(lint): replace spawn/wait with task.*, use time() in server code" },
      { "op":"status" }
    ]},
    { "type":"validate", "checks": [
      { "op":"cmd_contains", "cmd":"git log -1 --pretty=%s", "substr":"chore(lint): replace spawn/wait with task.*" }
    ]}
  ]
}
