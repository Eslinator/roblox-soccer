{
  "dry_run": false,
  "steps": [
    { "type": "run", "cmd": "git push -u origin bot/setup-connector" },
    { "type": "validate",
      "checks": [
        { "op": "cmd_contains", "cmd": "git ls-remote --heads origin bot/setup-connector", "substr": "refs/heads/bot/setup-connector" }
      ]
    }
  ]
}
